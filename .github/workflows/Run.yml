name: Run

on: 
  push:
  schedule:
  - cron: '0 23 * * *'
  workflow_dispatch:

jobs:
  run:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        apt install chromium-chromedriver
        python -m pip install --upgrade pip
        pip install requests
        pip install opencv-python
        pip install selenium
        pip install Pillow
    - name: 'Working'
      run: |
        sudo cp -p ./chromedriver.exe /usr/bin/
        chmod -R 777 /usr/bin/chromedriver.exe
    - name: run
      run: python text.py
